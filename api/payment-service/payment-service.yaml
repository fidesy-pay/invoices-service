type: google.api.Service
config_version: 3

http:
  rules:
    - selector: payment_service.PaymentService.CreatePayment
      post: /create-payment
      body: '*'
    - selector: payment_service.PaymentService.CheckPayment
      post: /check-payment
      body: '*'